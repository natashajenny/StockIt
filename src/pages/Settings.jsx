import React from 'react';

import { NavBar } from '../components';

export class Settings extends React.Component {
    render() {
        return (
        <div>
            <NavBar />
            <h1> Settings </h1>
        </div>
        );  
    }
}
